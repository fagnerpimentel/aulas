\documentclass[border=3pt]{standalone}
\input{preamble}
\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm,
    gsq/.style={draw=black!75, line width=0.7pt, minimum size=3.4mm, inner sep=0pt},
    gci/.style={draw=black!75, line width=0.7pt, circle, minimum size=3.4mm, inner sep=0pt}]

  \def\dy{2.15}   % espaçamento entre linhas

  % linhas finas de separação (entre as ilustrações)
  \foreach \k in {1,...,5}{
    \draw[black!12, line width=0.5pt] (-0.25,{-\k*\dy+\dy/2}) -- (4.35,{-\k*\dy+\dy/2});
  }

  % ===================== 0. PROXIMIDADE =====================
  \begin{scope}[shift={(0,{-0*\dy})}]
    \foreach \gx in {0,1.7}{ \foreach \i in {0,1,2}{ \foreach \j in {0,1}{
      \node[gsq] at ({\gx+0.5*\i},{0.5*\j-0.25}) {}; }}}
  \end{scope}

  % ===================== 1. BOA CONTINUIDADE =====================
  \begin{scope}[shift={(0,{-1*\dy})}]
    % dois traços que se cruzam -> lidos como duas linhas contínuas (não como "V"s)
    \foreach \t in {0,...,13}{
      \pgfmathsetmacro\x{0.15+0.30*\t}
      \node[gci, minimum size=2.6mm] at (\x,{ 0.62 - 0.31*\x}) {};
      \node[gci, minimum size=2.6mm] at (\x,{-0.62 + 0.31*\x}) {};
    }
  \end{scope}

  % ===================== 2. SIMETRIA =====================
  \begin{scope}[shift={(0,{-2*\dy})}]
    \draw[black!75, line width=0.9pt] (0,-0.7) -- (1.3,0.6) -- (0,0.6) -- (1.3,-0.7) -- cycle;
    \draw[black!75, line width=0.9pt] (2.3,-0.7) -- (3.7,0.6) -- (2.6,0.6) -- cycle;
    \draw[black!75, line width=0.9pt] (3.7,-0.7) -- (2.95,0.05) -- (3.9,-0.05) -- cycle;
  \end{scope}

  % ===================== 3. SIMILARIDADE =====================
  \begin{scope}[shift={(0,{-3*\dy})}]
    \foreach \i in {0,1,2,3}{ \foreach \j in {0,1,2}{
      \ifodd\i \fill[black!70] ({0.56*\i},{0.5*\j-0.5}) circle (1.7mm);
      \else    \node[gsq] at ({0.56*\i},{0.5*\j-0.5}) {}; \fi }}
  \end{scope}

  % ===================== 4. DESTINO COMUM =====================
  \begin{scope}[shift={(0,{-4*\dy})}]
    \foreach \i in {0,1,2,3}{
      \foreach \s in {0,...,4}{ \draw[black!30, line width=0.5pt] ({1.0*\i-0.11*\s},0) circle (2.0mm); }
      \fill[black!70] ({1.0*\i},0) circle (2.0mm);
      \draw[->, dblue, line width=0.8pt] ({1.0*\i+0.32},0) -- ++(0.42,0);
    }
  \end{scope}

  % ===================== 5. FECHO =====================
  \begin{scope}[shift={(0,{-5*\dy})}]
    \foreach \a in {28,52,...,332}{
      \node[gci, minimum size=2.6mm] at ({1.05+0.95*cos(\a)},{0.95*sin(\a)}) {};
    }
  \end{scope}

\end{tikzpicture}
\end{document}
