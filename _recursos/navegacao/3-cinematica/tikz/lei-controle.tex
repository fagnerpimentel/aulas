\documentclass[border=3pt]{standalone}
\input{preamble}
\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]

  \node[dbox, dacc, font=\scriptsize, align=center, text width=6.8cm] at (0,1.7)
     {\textbf{lei de controle linear}\\[3pt]
      $v = k_\rho\,\rho$ \qquad $\omega = k_\alpha\,\alpha + k_\beta\,\beta$\\[3pt]
      \scriptsize equilíbrio único em $(\rho,\alpha,\beta) = (0,0,0)$, sem singularidade em $\rho=0$};

  \node[dbox, draw=dgreen, font=\scriptsize, align=center, text width=6.8cm] at (0,-1.35)
     {\textbf{condições de estabilidade}\\[3pt]
      comum:\; $k_\rho > 0$ \quad $k_\beta < 0$\\[2pt]
      local (exponencial):\; $k_\alpha - k_\rho > 0$\\[2pt]
      forte (robusta):\; $k_\alpha + \tfrac{5}{3}k_\beta - \tfrac{2}{\pi}k_\rho > 0$\\[3pt]
      \scriptsize ex.: $k = (k_\rho, k_\alpha, k_\beta) = (3,\ 8,\ -1{,}5)$};

  % espiral de trajetórias
  \begin{scope}[shift={(9.5,0)}]
    \draw[dguide] (0,0) circle (2.0);
    \foreach \a in {0,45,...,315}{
      \draw[dcurve, line width=0.9pt]
        (\a:2.0) .. controls ({\a-40}:1.3) and ({\a-90}:0.7) .. (0,0);
    }
    \fill[dorange] (0,0) circle (0.09);
    \node[dsub, font=\scriptsize, anchor=north] at (0,-2.2) {trajetórias suaves ao objetivo (do círculo unitário)};
  \end{scope}

\end{tikzpicture}
\end{document}
