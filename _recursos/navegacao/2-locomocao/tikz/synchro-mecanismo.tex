\documentclass[border=3pt]{standalone}
\input{preamble}
\begin{document}
\begin{tikzpicture}[x=1cm,y=1cm]

  \draw[dguide] (0,0) circle (2.6);

  \foreach \a in {90,210,330} {
    \draw[dbody, dacc, line width=1.4pt] ({2.2*cos(\a)-0.35},{2.2*sin(\a)}) rectangle ++(0.7,0.35);
  }

  \fill[dorange] (0,0) circle (0.12);
  \node[dorange, font=\scriptsize, anchor=north] at (0,-0.2) {motor de direção};
  \draw[dorange, line width=1pt, dashed] (0,0) circle (1.3);
  \node[dorange, font=\scriptsize] at (1.55,1.1) {correia de direção};

  \fill[dblue] (0,1.3) circle (0.10);
  \node[dblue, font=\scriptsize, anchor=south] at (-1.0,1.85) {motor de tração};
  \draw[dblue, line width=1pt] (0,1.3) -- (0,2.2);
  \draw[dblue, line width=0.8pt] ({2.2*cos(90)},{2.2*sin(90)}) -- (0,1.3);
  \draw[dblue, line width=0.8pt] ({2.2*cos(210)},{2.2*sin(210)}) -- (0,1.3);
  \draw[dblue, line width=0.8pt] ({2.2*cos(330)},{2.2*sin(330)}) -- (0,1.3);

  \node[dsub, font=\scriptsize, align=center, text width=13.5cm] at (0,-3.4)
     {Apenas \textbf{dois motores} controlam as três rodas: um define a velocidade (correia de tração) e outro esterça todas as rodas \textbf{juntas} --- o chassi nunca reorienta};

\end{tikzpicture}
\end{document}
